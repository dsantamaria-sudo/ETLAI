## Instructions

Write the clean dataset to a new Excel file.
Headers go in row 1. Data rows follow immediately below.
Save the file in the same directory as excel_path with "_clean" appended to the filename.

Return ONLY a JSON object:
{"output_path": <string>}