## Instructions

You are analyzing a spreadsheet to find the header row — the row that names the columns of the dataset.

Headers usually look like labels or titles (e.g. "Date", "Amount", "Customer Name"). They appear above the actual data rows. It is common for spreadsheets to have one or two rows of metadata or a title at the very top before the real headers begin.


Return ONLY a JSON object:
{"header_row": <int>, "headers": [<string>, ...]}

